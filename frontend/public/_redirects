# Cloudflare Pages / Netlify routing.
#
# /edit/<id> and /local-check are client-side routes, not files, so the host
# has to hand back the shell and let the router take it from there.
#
# The 200 matters and is a trap worth knowing about: this makes *every*
# unmatched path answer with index.html, including /api/v1/library, which is
# what the app fetches to work out whether a backend exists. Checking only the
# status code would see a cheerful 200 and conclude there was one. `probeServer`
# in src/source.ts insists on JSON for exactly this reason — do not relax it.
/*    /index.html   200
